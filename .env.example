# Contract addresses — populated after deploy (mnemos-contract repo)
NEXT_PUBLIC_REGISTRY_ADDRESS=0x
NEXT_PUBLIC_MARKETPLACE_ADDRESS=0x

# 0G Chain RPC
NEXT_PUBLIC_OG_RPC_URL=https://evmrpc-testnet.0g.ai
NEXT_PUBLIC_OG_CHAIN_ID=16600

# Backend API base URL (mnemos-backend, default port 3001)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# RainbowKit / WalletConnect project ID
# Get one at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
